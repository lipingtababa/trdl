{
	"name": "trdl",
	"version": "0.1.0",
	"private": true,
	"license": "commercial",
	"scripts": {
		"deploy": "wrangler deploy src/index.ts",
		"dev": "wrangler dev --remote --ip=127.0.0.1  --port=12345 --log-level=debug &",
		"test": "vitest run"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20231016.0",
		"typescript": "^5.0.4",
		"vitest": "^1.3.1",
		"wrangler": "^3.0.0"
	}
}
